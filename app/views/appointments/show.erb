<h1>Appointment with: <%=link_to @appt.doctor.name, doctor_path(@appt.doctor)%></h1>
<h2><%= link_to @appt.patient.name, patient_path(@appt.patient)%></h2>
<h3>Appointment time:</h3>
<%= @appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> 
